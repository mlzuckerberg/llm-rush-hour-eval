# Copy to .env (gitignored). Set provider budget caps before spending money.

# OPENAI_API_KEY=sk-...
# Optional overrides if not using config.yaml:
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o-mini
# LLM_TEMPERATURE=0
# Cap completion size (safety if the model rambles; raise if plans get truncated):
# LLM_MAX_OUTPUT_TOKENS=2048
